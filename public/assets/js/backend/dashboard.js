define(['jquery', 'bootstrap', 'backend', 'addtabs', 'table'], function ($, undefined, Backend, Datatable, Table, Echarts, undefined, Template) {

    var Controller = {

    };

    return Controller;
});